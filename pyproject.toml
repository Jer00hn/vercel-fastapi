[project]
name = "subscription-server"
version = "0.1.0"
description = "FastAPI subscription management service using Upstash Redis"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "httpx",
    "pydantic",
    "uvicorn",
]
